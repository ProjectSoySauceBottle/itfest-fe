export const metadata = {
  title: "Dashboard",
  description: "",
};

function DashboardLayout({ children }) {
  return (
    <div>
      DashboardLayout
      <main>{children}</main>
    </div>
  );
}
export default DashboardLayout;
